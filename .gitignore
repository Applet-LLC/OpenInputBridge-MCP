node_modules/
dist/
*.log

# C helper build output
helper/x64/
helper/Debug/
helper/Release/
helper/*.obj
helper/*.exe
helper/*.pdb
helper/*.ilk

# Packaged native binary (populated by release.yml before `npm publish`,
# never committed to source control)
bin/

# VS/MSBuild
.vs/
*.user

npm-debug.log*

# AI assistant session history (local only, not project source)
.specstory/
.vscode/
