# セキュリティ

このドキュメントは、OpenInputBridge-MCPが持つリスクと、実装済みの安全機構をまとめたものです。OpenInputBridge本体固有のリスク(ドライバのACL設計等)は本体の [`docs/SECURITY_CONSIDERATIONS.md`](https://github.com/Applet-LLC/OpenInputBridge/blob/main/docs/SECURITY_CONSIDERATIONS.md) を参照してください。本ドキュメントは、**その能力をAIエージェントに対して簡単な入口として公開する**ことで生じる、MCPサーバー固有のリスクに焦点を当てます。

## 1. このツールが技術的に何をできるか

OpenInputBridgeドライバのコントロールデバイス(`\\.\interceptionNN`)は、Interceptionプロトコル互換性維持のため `Everyone` に汎用読み書き権限を付与しています。したがって:

- ドライバの**インストール自体**には管理者権限が必要
- しかし**インストール後**は、非昇格プロセスからでもシステム全体のキーボード/マウス入力を注入(v1では読み取り・監視は不可、[README.mdの「できること」](README.md#できることv1ツール一覧)参照)できる

本MCPサーバーは、この能力をJSON-RPCベースの構造化されたツール呼び出しとして、AIエージェントに一段簡単にアクセスできる形で提供します。これは「無昇格プロセスからの入力注入」という既存のリスクプロファイル([OpenInputBridge本体のSECURITY_CONSIDERATIONS.md](https://github.com/Applet-LLC/OpenInputBridge/blob/main/docs/SECURITY_CONSIDERATIONS.md)参照)を、AIエージェント経由という新しい入口から利用可能にするものであり、**プロンプトインジェクションや暴走したエージェントによる誤操作・悪用のリスクを増幅させ得る**という点が、本体にはないMCP固有の懸念です。

## 2. 実装済みの安全機構

### 2.1 明示的なarm(`enable_input_control`)

MCPクライアント自体のツール許可UIとは別に、送信系ツールを使う前に専用ツールを1回呼ぶことを必須にしています。サーバー起動直後は全ての送信系ツールが拒否されます。「MCPクライアントの許可 = 即実行可」という一段だけの同意にせず、このドライバ固有の強力さに見合ったもう一段の確認を意図的に挟んでいます。

### 2.2 レート制限

スライディングウィンドウ方式で、既定10秒間に最大500入力イベントまで(環境変数で変更可)。プロンプトインジェクション等で暴走したエージェントが入力を連射し続ける事態を防ぎます。

### 2.3 送信専用設計(読み取り・監視系ツールなし)

`IOCTL_READ`/`interception_receive` 相当の、物理入力の内容を捕捉してAIエージェントに渡すツールは実装していません。「MCP経由でAIがシステム全体のキー入力を盗聴できる」という最も深刻な悪用シナリオを、v1の設計から構造的に排除しています。

### 2.4 stdioトランスポートのみ

ネットワークリスナーは一切持ちません。MCPクライアントがローカルでサブプロセスとして起動する使い方のみをサポートしており、認証・TLS・リモート攻撃面といった懸念が構造的に発生しません。

### 2.5 排他モードの安全機構

`enable_exclusive_input_mode` は、物理キーボード/マウスを丸ごと無効化する(捕捉して破棄する)機能であり、通常の送信ツールより誤操作時の影響が大きいため、追加の安全機構を設けています。

- **既定で無効・明示的opt-inのみ**: 呼ばない限り有効化されません
- **ウォッチドッグによる自動解除**: MCPサーバーからヘルパー(`oib_bridge.exe`)への定期的なハートビートが一定時間(既定5秒、`watchdogTimeoutMs`で設定可、最大300秒)途絶えると、ヘルパーは自律的に排他モードを解除します。エージェントのハング・MCPサーバー自体の異常終了で物理入力が永久にロックされる事態を防ぎます
- **`disable_exclusive_input_mode` は常に呼び出せる**: arm状態・レート制限を意図的にバイパスしており、エスケープハッチとして機能します
- **プロセス終了時の自動解放**: `oib_bridge.exe` が正常終了・異常終了・強制終了いずれの理由で終了しても、OpenInputBridgeドライバ自身のハンドルクローズ時クリーンアップにより、そのプロセスが保持していた捕捉状態・フィルタ設定は即座に解放され、実入力ストリームに自動的に復帰します

#### 唯一の外部からの緊急解除手段: `oib_bridge.exe` プロセスの終了

Interceptionプロトコルでは、あるハンドルのフィルタ/優先順位(precedence)を変更できるのは、**そのハンドルを開いているプロセス自身だけ**です。別プロセスが外部から割り込んで強制的にリセットする手段はプロトコル上存在しません。

したがって、MCPサーバー・AIエージェント側が完全に応答不能になった場合でも、**タスクマネージャ等で `oib_bridge.exe` プロセスを終了させれば、ドライバ側のクリーンアップにより即座に物理キーボード/マウス入力が復元されます**。これが唯一かつ確実に機能する緊急解除手段です。無人のテスト実行環境で排他モードを使う場合は、この手順(プロセス名: `oib_bridge.exe`)を運用者が把握しておくことを強く推奨します。

なお、OSレベルのセキュアアテンションシーケンス(Ctrl+Alt+Del)は、このドライバより下位のレイヤーでWindowsが処理するため、排他モード中も引き続き機能します(本ドライバはWindowsのSAS処理を妨げません)。

### 2.6 監査ログ・トースト通知(OpenInputBridge本体の機能)

OpenInputBridge本体のOSSインストーラには、SACL方式の監査ログ(セキュリティイベントログ4656でどのプロセスがいつコントロールデバイスを開いたかを記録)と、トースト通知機能があります。**本MCPサーバーおよびそのヘルパー(`oib_bridge.exe`)がコントロールデバイスを開く動作もこの仕組みで捕捉されます。** AIエージェントと併用する環境では、この監査ログ・トースト通知を有効化することを強く推奨します。詳細は[本体のREADME/docs/DECISIONS.md](https://github.com/Applet-LLC/OpenInputBridge)を参照してください。

### 2.7 フォアグラウンドウィンドウガード(未実装)

パスワード入力欄・認証系ウィンドウを検知して送信を拒否する、事故防止のベストエフォート機構は、v1では未実装です(将来検討)。実装されたとしてもセキュリティ境界としては機能しない前提で使用してください。

## 3. 検知可能性について

OpenInputBridgeが使用するInterceptionプロトコル自体(デバイス命名 `\\.\interceptionNN` を含む)は、Interceptionを使ったツールを既に警戒しているアンチチート等のコミュニティにおいて、検知対象になり得ます。「検知されない」ことを技術的に保証するものではありません。詳細は[OpenInputBridge本体のSECURITY_CONSIDERATIONS.md](https://github.com/Applet-LLC/OpenInputBridge/blob/main/docs/SECURITY_CONSIDERATIONS.md)を参照してください。

## 4. 利用規約・法令遵守は利用者の責任

本ツールは、**自分が権限を持つ/管理しているテスト環境**(自社製品のQA、CI、専用テスト機)での利用を想定しています。以下のような用途は想定しておらず、利用者自身の責任において、対象ソフトウェアの利用規約・ゲームのアンチチート規約・関連法令に違反しないことを確認してください。

- 権限を持たない他者のPC・アカウントでの利用
- ゲームや他社アプリのアンチチート/利用規約の回避目的での利用
- 監視・キーロギング目的での利用(v1にはそもそも読み取り・監視系ツールがありませんが、将来のバージョンで追加されたとしても、無断監視への利用は禁止です)

## 5. 脆弱性の報告

セキュリティ上の問題を発見した場合は、公開Issueではなく、GitHubリポジトリの Security タブから Private vulnerability reporting、または[リポジトリ管理者](https://github.com/Applet-LLC)宛に非公開の連絡手段でご連絡ください。

## 参照

- [OpenInputBridge本体 docs/SECURITY_CONSIDERATIONS.md](https://github.com/Applet-LLC/OpenInputBridge/blob/main/docs/SECURITY_CONSIDERATIONS.md)
- [OpenInputBridge本体 docs/PROTOCOL.md](https://github.com/Applet-LLC/OpenInputBridge/blob/main/docs/PROTOCOL.md)
- [README.md](README.md) の「AIエージェントが知っておくべき仕様」
- [`src/safety.ts`](src/safety.ts)(arm/レート制限の実装)
- [`helper/oib_bridge.c`](helper/oib_bridge.c)(排他モード・ウォッチドッグの実装)
